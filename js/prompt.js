// prompt.js — 프롬프트 조립 엔진 (M2에서 구현)
// TODO(M2): 가이드라인 JSON + 사용자 입력 → system 프롬프트 조립
//   구조: [기본 정보]/[용어 규정]/[출제 불가]/[추가 출제 가능]/[난이도 상 전용]/[문항 형식]/[출력 형식(JSON 스키마)]
function buildGenerationPrompt(guideline, options) {
  throw new Error("M2에서 구현 예정");
}

// TODO(M3): 검토 모드 프롬프트 (판정 첫 줄 규칙 + 악마의 변호인 모드)
function buildReviewPrompt(guideline, options) {
  throw new Error("M3에서 구현 예정");
}
